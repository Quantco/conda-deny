[project]
name = "my-project"
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.conda-deny]
license-whitelist = "tests/test_lockfile_pattern/license_whitelist.toml"
lockfile = "tests/test_lockfile_pattern/**/*.lock"
