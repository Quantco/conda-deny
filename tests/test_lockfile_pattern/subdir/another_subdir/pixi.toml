[project]
name = "another-subdir"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
vhs = "*"

# Avoid tooling trying to autoupdate this pixi.toml
[tool.update]
autoupdate-schedule = "never"