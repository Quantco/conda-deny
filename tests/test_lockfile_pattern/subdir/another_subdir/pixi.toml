[project]
name = "another-subdir"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
vhs = "*"

# Avoid GitHub tooling trying to update this pixi.toml
[tool.update]
autoupdate-schedule = "never"