[project]
name = "subdir"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
rust = "==1.77.2"
openssl = "3.*"
pkg-config = "*"
k9s = "*"

# Avoid tooling trying to autoupdate this pixi.toml
[tool.update]
autoupdate-schedule = "never"