[package]
name = "local-build-local-pkg"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-cmake", version = "==0.3.10" }

[package.run-dependencies]
curl = "==8.17.0"

# Avoid GitHub tooling trying to update this pixi.toml
[tool.update]
autoupdate-schedule = "never"